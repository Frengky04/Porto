// app/ui/fonts.ts
import { Lusitana,Inter,Bebas_Neue,Kanit,Poppins } from 'next/font/google';

export const lusitana = Lusitana({
  subsets: ['latin'],
  weight: ['400', '700'],
});

export const inter = Inter({
  subsets: ['latin'],
  weight: ['400', '700'],
});

export const bebas_Neue = Bebas_Neue({
  subsets: ['latin'],
  weight: ['400',],
});

export const kanit = Kanit({
  subsets: ['latin'],
  weight: ['400',],
});

export const poppins = Poppins({
  subsets: ['latin'],
  weight: ['400',],
});